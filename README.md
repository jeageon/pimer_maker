# Primer Maker

`pimer_maker`는 기존 도구의 핵심 로직을 재활용해
GenBank(`.gb`) 파일을 입력받아
`프라이머 간섭 구간`과 `프라이머 후보`를 계산하고
결과를 주석(Feature) 포함 `GenBank`로 내려주는 도구입니다.

## 실행 방법


### Windows
C:\Program Files\pimer_maker-v0.1.8 폴더에서
run-PimerMaker-WebUI.bat 실핼
*필요시 해당 파일로 바로가기 만들기
